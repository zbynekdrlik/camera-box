# Rust build artifacts
/target/
Cargo.lock

# Deploy-download staging dir (gh run download -n camera-box-linux-amd64 --dir ./dist)
/dist/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Build artifacts
*.img
*.img.xz
*.tar.gz

# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# MCP config (symlink to ~/.claude/mcp-configs/)
.mcp.json

# cargo-mutants output
mutants.out/
mutants.out.old/

# frame-probe loopback run artifacts (LOCAL_OUT in scripts/loopback-e2e.sh)
frame-probe-*.json

# Python bytecode cache (from scripts/*.py)
__pycache__/
*.pyc

# Autopilot/session local logs
.claude/command-history.log
dist-genlock/
.playwright-mcp/

# Session-local deploy staging + verify scratch (#136)
deploy-136/
deploy-136b/
dist-136/
.obs-verify/

# Session-local deploy staging (#105/#150 grab-record validation)
dist-150/
deploy-150/

# Session-local Windows genlock-build artifact download (#111 burn DLL deploy)
winartifact/
deploy-111/
dist-111/

# Session-local verdict re-run + probe-tools artifact scratch (#202)
/verdict-rerun-out/
/dist-probe/
/recording-verdict-run/

# Session-local proof-run scratch + CI probe-binary downloads (proof runs; #110/#184/#213)
# gh run download --dir target dirs for the probe-tools artifacts (downloaded CI
# binaries — never committed). probe-linux/ + probe-windows/ are the per-OS download
# dirs (CLAUDE.md Build & Deploy); the rest are session-local scratch.
/probe-bins/
/probe-bins-ci/
/probe-linux/
/probe-win/
/probe-win-ci/
/probe-win-fresh/
/probe-windows/
/proof-*/
/obs184-dll/

# Stray stdout/stderr redirect captures from ad-hoc debug runs (#320). Root-anchored +
# narrow so they never get committed again (err_a.txt / outB.txt style).
/err_*.txt
/out[A-Z].txt

# Windows build artifacts pulled for rig runs (never commit)
dist-win/

# issue 930 -- lipsync-test asset binaries (re-derived from assets/lipsync/PROVENANCE.md's
# pinned URL+sha256 by scripts/lipsync-asset.sh; never committed -- PROVENANCE.md IS committed).
assets/lipsync/*.ogv
assets/lipsync/*.mp4
assets/lipsync/*.jsonl
assets/lipsync/*.jpg

# issue 929 -- ASRC quality A/B bench compiled binary (built fresh by run_ab.sh, never committed;
# the .c source + RESULTS.md are the committed artifacts)
/scripts/asrc-quality-bench/asrc_ab_harness
