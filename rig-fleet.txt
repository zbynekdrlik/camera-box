# rig-fleet.txt -- #827 repo-level DEFAULT for CAMBOX_OFFLINE_ACK.
#
# Read by scripts/recording-e2e.sh (via scripts/lib/cambox-offline-ack.sh's
# cambox_offline_ack_effective) whenever CI's own CAMBOX_OFFLINE_ACK is empty -- this is the
# fallback the automatic pull_request E2E gate uses (it carries no workflow_dispatch inputs). A
# manual `workflow_dispatch` run's `offline_ack` input, when non-empty, OVERRIDES this file
# outright.
#
# Format: one "box:reason" per line. Blank lines and lines starting with '#' are ignored. Keep
# this in sync with the PHYSICAL rig state -- a box named here that is actually reachable AND
# healthy fails the run LOUDLY as a stale ack (#758/#827); remove the line the moment the box is
# back in the fleet.
#
# issue 1013: imag-nb (the 60fps IMAG cutter, 10.77.9.182) is ACKABLE here too, as the box name
# "imag" -- `imag:notebook-taken-after-event-<date>`. When the notebook is physically absent
# (taken away after an event) this lets the whole E2E gate RUN with the imag leg SKIPPED (a loud,
# report-only IMAG-LEG-SKIPPED note per step + an IMAG-LEG-NOT-VERIFIED marker) instead of dying at
# the [0/8] reachability preflight. Exactly like a cam box, an acked-but-reachable imag is a STALE
# ack that fails the run -- so add the `imag:` line ONLY while it is genuinely off the network, and
# remove it the moment the notebook is back. (Remote wake / WoL for imag is separate hardware work,
# tracked apart from this gate change; issue 1053 is its strih/stream counterpart.)
#
# Current physical state (2026-08-27, issue 1198 cam1+cam2 restoration -- owner ruling: the cards
# are fine, they are not being replaced):
# cam1: RESTORED 2026-08-27 (issue 1198) -- back in CAMERA_ACTIVE_SET (and CAMERA_ALIGN_SET), its
#   former offline ack REMOVED. The 2026-08-22 "HARDWARE-DEFECTIVE" diagnosis (issue 1110: chronic
#   over-rate 61.5->63.1 fps captured, constant 4 corrupted/5s, -EPROTO; USB re-auth does not cure)
#   was built from an EPISODE, not a permanent card state -- a live read-only journal check on
#   2026-08-27 (production running) showed cam1 at 60.0/61.4 fps, 4 capture-dropped/5s, 1 corrupted,
#   colour, no STUCK/self-heal/LATCH marker. The owner refused the physical swap outright ("tie dve
#   karty su vpohode nebudem ich menit za ine lebo su uplne funkcne"). cam1 is again the FIRST
#   strih-routable member of CAMERA_ACTIVE_SET, so camera_source_box() resolves the source back to
#   cam1 (byte-identical to the pre-#1110 behaviour). The historical episode stays real; its root
#   cause is now tracked as OUTSIDE the card (USB/power/splitter/kernel/thermal) on issue 1198 --
#   re-ack this line + drop "cam1" from CAMERA_ACTIVE_SET/CAMERA_ALIGN_SET again ONLY if a fresh,
#   reproduced episode demands it.
# History: cam1 was first retired 2026-08-19 (issue 1134, owner order #1130) and RETURNED the same day
#   (issue 1130 read the judder as a deployed emit-gate regression, fixed by dev.481, not the card);
#   RE-RETIRED 2026-08-22 (issue 1110, the chronic over-rate episode above); RESTORED again 2026-08-27
#   (issue 1198, this time by owner ruling that the card itself is fine).
# cam3: RE-ACTIVATED 2026-08-13 (issue 939) -- in CAMERA_ACTIVE_SET, a measured preflight
#   target again; its former ack line is GONE (a healthy acked box trips the stale-ack guard).
# cam4: ON AIR but OUTSIDE the measured set -- ack RESTORED 2026-08-07 with this exact reason.
#   The box is healthy (preflight contract passes on 10.77.9.64) and its feed genuinely renders
#   on strih, so the 2026-08-06 "removed from rig" reason was stale. BUT cam4 is not in
#   camera-set.sh's CAMERA_ACTIVE_SET (default "cam1 cam2 cam3" as of issue 1198), so the burn sweep produces no
#   delivery samples for it and the all-cambox A/V derivation is STRUCTURALLY impossible --
#   E2E run 31157261313 proved it live: cam4 judged, windows=0 candidates=0, UNKNOWN, gate FAIL
#   forever. Until cam4 is deliberately re-entered into CAMERA_ACTIVE_SET (the issue-753-class
#   re-integration work: strih input/scene wiring + sweep membership), the honest state is
#   "visible on air, unmeasurable, excluded from A/V judging" -- which is this ack. The stale-ack
#   guard does not fire on it because cam4 is not a preflight target while outside the active set.
# Without the remaining acks the re-armed A/V-offset gate (issue 861) fails closed forever on
# their UNKNOWN (0 samples) verdicts -- an unacked absent camera must fail the gate by design
# (#855).
cam4:on-air-but-outside-measured-set-2026-08-07
cam5:powered-off-2026-07-27
cam6:powered-off-2026-07-27
cam7:powered-off-2026-07-27
