#!/usr/bin/env python3
"""#312 Phase-2 — PURE switch-schedule generation for the all-cambox sweep (STUB — RED).

Real implementation lands in the GREEN commit. These stubs exist so the unit tests
(tests/python/test_switch_schedule.py) reference real symbols and FAIL on behaviour
(canonical RED) rather than on an import error.
"""


def parse_sweep(spec):
    return []


def n_segments(segment_secs, duration):
    return 0


def plan_segments(sweep, segment_secs, duration):
    return []


def build_switch_schedule(sweep, segment_secs, duration, start_ns, boundaries_ns):
    return []
