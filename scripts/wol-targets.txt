# scripts/wol-targets.txt -- #1053 Wake-on-LAN target table for scripts/wake-box.sh.
#
# Format: box  ip  mac   (whitespace-separated; blank lines and `#` comment lines ignored).
#
# MACs are NOT secrets -- read live from each box's ACTIVE NIC (Windows `Get-NetAdapter` MacAddress,
# the adapter carrying the box IP) over ssh on 2026-08-17. IPs per targets.md. The subnet-directed
# broadcast address is DERIVED from the /24 (a.b.c.255) by scripts/lib/wol.sh wol_table_lookup, never
# stored here, so each box has a single source of truth.
#
# box     ip             mac
strih     10.77.9.202    5C:6A:80:F6:6C:F7
stream    10.77.9.204    E8:9C:25:CE:B6:EA
# imag-nb (#1103): the imag projection notebook. NIC is a USB r8152 Realtek dongle (enx<mac>) whose
# MAC was live-read on 2026-08-18 (`cat /sys/class/net/<nic>/address`, `Supports Wake-on: pumbg`).
# The imag ROLE permanently owns .182 (scripts/imag-host.sh); WoL from a full shutdown additionally
# needs the box's BIOS to keep the USB port powered in standby -- see docs/wake-on-lan.md.
imag-nb   10.77.9.182    6C:1F:F7:66:15:4B
