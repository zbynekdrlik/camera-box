# ===== #208 extract-strih-partial ON the strih box (decode where the video is) =====
# strih box: 10.77.9.202  (MCP: win-strih)
#
# STEP 1 (once): upload the CI-built verdict to the box
#   win-strih FileUpload  path='C:\camera-box\recording-verdict.exe'  <- probe-tools-windows-amd64/recording-verdict.exe
#
# STEP 2: extract the strih PARTIAL ON the box against the LOCAL recording (NO copy anywhere):
#   win-strih Shell:
$env:RUST_LOG="info"; & "C:\camera-box\recording-verdict.exe" "--extract-partial" "strih" "--strih" "C:\x\rec.mkv"
#
# STEP 3: pull back the small partial JSON (ids+timestamps) AND the #186 pixel-proof PNGs:
#   win-strih FileDownload  path='<the --out partial JSON inside C:\camera-box\verdict-out>'
#   win-strih FileDownload  path='<the <partial>-pixels dir beside the partial>'   # #186 pixel proofs (a handful; absent on a clean run)
#
# dev1 receives ONLY the tiny partial JSON + the handful of flagged-frame PNGs. The
# C:\x\rec.mkv stays on the strih box and is NEVER copied — not to the stream
# box, not to dev1 (#208). The merge derives the same <partial>-pixels dir to locate the proofs.
